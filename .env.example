POSTGRES_URL="your_postgres_connection_url"

# Copy this file to .env and replace the value above.
# Obtain your connection string from the Next.js App Router Course (Vercel / Neon DB setup section) or your own Postgres provider.
# Example format (Neon):
# postgres://USER:PASSWORD@HOST/dbname?sslmode=require

# After creating .env, restart the dev server.# Copy from .env.local on the Vercel dashboard
# https://nextjs.org/learn/dashboard-app/setting-up-your-database#create-a-postgres-database
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# `openssl rand -base64 32`
AUTH_SECRET=
AUTH_URL=http://localhost:3000/api/auth